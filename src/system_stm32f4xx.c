/* Minimal system file placeholder.
   Add system clock init or CMSIS device support here if needed. */
#include <stdint.h>

void SystemInit(void)
{
    /* Optionally configure FPU, clocks, vector table offset, etc. */
}
